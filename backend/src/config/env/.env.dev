# =============================================================================
# FindClass Backend - Development Environment
# =============================================================================
# This file is auto-generated for local development
# DO NOT commit to version control with real credentials
# =============================================================================

# Environment
NODE_ENV=development
PORT=3000

# AWS Configuration (LocalStack)
AWS_REGION=ap-southeast-2
AWS_ACCESS_KEY_ID=local
AWS_SECRET_ACCESS_KEY=local

# S3 (LocalStack)
S3_ENDPOINT=http://localhost:4566

# SMTP (Development email service)
# For local development, you can use MailHog or a test SMTP server
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_API_PORT=8025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=no-reply@findclass.nz

# Frontend URL
FRONTEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# JWT (Development secrets - change for production!)
JWT_SECRET=dev-secret-key-at-least-32-characters-long
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Logging
LOG_LEVEL=debug

# LocalStack endpoint
LOCALSTACK_ENDPOINT=http://localhost:4566

# Seed sample data
SEED_SAMPLE_DATA=false
