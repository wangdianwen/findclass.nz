# =============================================================================
# FindClass Frontend - Staging Environment
# =============================================================================
# This file is for staging/preview deployments
# Connects to local backend API via nginx proxy
# =============================================================================

# Environment
NODE_ENV=staging
VITE_APP_TITLE=FindClass.nz (Staging)

# API Configuration - use relative path for nginx proxy
VITE_API_URL=/api/v1
VITE_ENABLE_MSW=false
VITE_APP_ENV=staging
